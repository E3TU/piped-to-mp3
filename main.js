const fs = require("fs");
const ytdl = require("ytdl-core");

// let url = "https://youtube.com/watch?v=XW6-00SqPHM";

// ytdl(url, {filter: "audioonly", quality: "highest" })
//     .pipe(fs.createWriteStream("video.mp3"));